@model IEnumerable<QLBanDongHo60135761.Models.LoaiDongHo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>DANH SÁCH LOẠI ĐỒNG HỒ</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-primary", @style = "width: 120px;" })
</p>
<table class="table table-bordered">
    <tr style="font-size: 16px; background-color: #E0FFFF; ">
        <th>
            Mã loại đồng hồ
        </th>
        <th>
            Tên loại đồng hồ
        </th>
        <th>Chức năng</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr style="font-size: 16px;">
            <td>
                @Html.DisplayFor(modelItem => item.MaLoaiDH)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiDH)
            </td>
            <td>
                @Html.ActionLink("Cập nhật", "Edit", new { id = item.MaLoaiDH }, new { @class = "btn btn-success", @style = "width: 120px;" })
                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaLoaiDH }, new { @class = "btn btn-info", @style = "width: 120px;" })
                @Html.ActionLink("Xoá", "Delete", new { id = item.MaLoaiDH }, new { @class = "btn btn-danger", @style = "width: 120px;" })
            </td>
        </tr>
    }

</table>
